name: app_ui
description: UI Toolkit for the Biometric Flutter Web Application
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  another_flushbar: ^1.12.30
  cached_network_image: ^3.4.1
  flex_dropdown: ^0.2.0
  flutter:
    sdk: flutter
  flutter_svg: ^2.2.0
  intl: ^0.20.2
  svg_image: ^0.0.5

dev_dependencies:
  build_runner: ^2.4.13
  flutter_test:
    sdk: flutter
  very_good_analysis: ^8.0.0

flutter:
  generate: true

